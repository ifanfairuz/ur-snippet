import { CopyclipDirective } from './copyclip.directive';

describe('CopyclipDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyclipDirective();
    expect(directive).toBeTruthy();
  });
});
